/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // i18n: {
    //   locales: ['es', 'en'],
    //   defaultLocale: 'es',
    // },
  };
  
  export default nextConfig;